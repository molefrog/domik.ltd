import styled from "@emotion/styled";

export default function PlaygroundPage() {
  return <Container>You can only see this in dev environment!</Container>;
}

const Container = styled.div`
  padding: 48px 16px 128px 16px;
  max-width: 700px;
  margin: 0 auto;
`;
